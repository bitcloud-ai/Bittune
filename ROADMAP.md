# Bittune 路线图

简体中文 ｜ [English](ROADMAP.en.md)

> 本文面向使用者和贡献者，描述 Bittune 公开的演进方向。阶段按"用户可获得的结果"划分，不绑定具体日期或功能清单；条目粒度刻意保持在主题层面。最近更新：2026-08-27。

状态图例：

| 标记 | 含义 |
|---|---|
| ✅ 已发布 | 在正式 Release 中可用 |
| 🚧 进行中 | 当前开发主线的活动目标 |
| 📋 规划 | 已纳入后续版本方向的明确承诺 |
| 💭 探索 | 方向成立但尚无进入条件，欢迎讨论 |

## 产品主线

Bittune 是本地优先的推理部署与优化智能体：它帮助已经拥有算力的用户——一台多卡机器或一个可用集群——找到模型更合适的部署方式，并通过真实测试持续优化。

所有规划围绕同一条主线推进：

```text
用户目标 → 智能体理解 → 少量受管领域工具 → 确定性工程核心
        → 真实部署与真实测量 → 可审计的实验记录 → 可复用的性能经验
```

两条不变的原则：

- LLM 负责理解、权衡与解释；一切数字必须来自确定性的工具与真实测量，绝不凭感觉估算。
- 先把单机做正确，再通过稳定边界扩展到多卡与集群；扩展的方式是新增实现，而不是推翻上层。

## ✅ 已发布

以最近一次 [Release](https://github.com/bitcloud-ai/BitTune/releases) 为准，当前基线包括：

- Linux x86_64 单机安装器、离线安装路径与 `bittune doctor` 环境自检。
- 多推理引擎（当前覆盖 vLLM 与 SGLang）的统一受管生命周期：受限配置创建、就绪检查、端点探测、日志读取与停止。
- 统一口径的性能压测与实测运行点推导，不将单次成功误报为最大容量。
- Run Record / Artifact 形式的执行证据：敏感字段自动脱敏，失败同样留痕。
- 调优实验的记录与比较，所有结论绑定环境指纹与配置 Hash。
- TUI 交互与会话恢复；可选接入管理员配置的只读 MCP 参考源。

## 🚧 进行中

- 将硬件检测、模型发现、部署与压测从单一实现拆分为带版本边界的确定性核心层，使同一条链路可以被独立测试、独立替换。
- 强化调优数据的保留策略：执行证据始终留在本地；任何导出或共享都必须经过显式授权与最小化处理。
- 在真实 GPU 环境冻结多引擎闭环的验收证据（基线、回归与回滚）。

## 📋 规划 · 下一个大版本

这一组目标合起来回答一个问题：**让外部依赖的接口变稳，让内部实现获得替换的自由。**

1. **领域契约公开化**
   环境事实、模型画像、负载画像、部署意图、压测结果与实验记录以带版本的公共契约表达；智能体面向契约工作，而不是面向某个引擎的具体参数。
2. **引擎适配器接口**
   新推理引擎通过实现适配器接入生命周期、观测与压测体系，无需改动智能体层。这会是面向社区的第一条清晰贡献路径。
3. **实验记录 v1**
   "部署 A → 基准 → 部署 B → 基准 → 对比"成为一等公民操作：结论可回放、可引用、可跨会话追溯。
4. **本地性能注册表 v0**
   在本地直接回答："同样的硬件、同样的模型、同样的负载，以前测过什么？"数据 schema 先于数据规模。
5. **报告与人读输出**
   调优报告形成机器可校验 + 人可直接阅读的双层格式，作为对外分享的基本单元。

## 🌤 Later · 中期主题

以下主题将在"规划"一组完成后依次进入排期，先后顺序由真实使用反馈决定：

- **多卡与拓扑感知**
  NUMA、卡间互联与通信瓶颈作为一等领域事实被采集，支撑并行策略的诊断与建议。
- **已有集群的受限纳管**
  面向用户已有的 Kubernetes GPU 集群提供能力发现与受管部署；沿用与单机完全相同的所有权、授权、证据与回滚语义。
- **分布式服务形态**
  在聚合式 serving 之外，建立分离式 Prefill/Decode 等分布式形态的规划、部署与评测能力。
- **有先验的搜索**
  以性能预测和历史先验缩小候选空间；预测一律经真实基准校验并记录偏差，把"大范围盲搜"变成"少而准的可验证实验"。最终状态不是取消压测，而是先预测数百个配置，再只真实验证最有价值的少数候选。
- **开放的数据交换格式**
  匿名化基准数据集与硬件画像的导入导出格式；在默认不出网的前提下，让社区可以交换可复核的性能知识。

## 💭 探索中

- NVIDIA 之外的加速硬件支持面（取决于生态成熟度与真实需求）。
- 本地 Web 控制台的可视化视图。
- 第三方智能体组件接入产品的标准插件机制。
- Windows / macOS 宿主体验（当前优先级最低）。

## 明确的非目标

非目标与路线同等重要，以下事项不会出现在近期任何版本中：

- Bittune 不安装集群基础设施，不自建调度器，不替代既有的资源管理系统。
- 外部运行的模型服务默认只读；只有 Bittune 创建并登记所有权的资源才被纳入管理。
- 不做常驻的生产监控 / 告警平台；持续观测是一个有独立进入条件的后续阶段。
- 托管型云端控制台、多设备集中运营与商业增值服务不在本仓库范围内，由官方发行版另行提供。
- 不承诺"任意硬件开箱即用"：每类新设备都要通过真实环境的身份与制品验证门槛后，才会声明支持。

## 跨阶段的长期约束

完整论述见 [`docs/architecture/`](docs/architecture/README.md)，此处摘录对所有阶段生效的部分：

- Agent 只有提案权；真实操作全部经由可授权、可取消、可恢复、可回滚的受管工具。
- 权限、资源所有权与证据是三个正交概念，不能用工具可见性混同表达。
- 每个重要结论都区分来源：declared / detected / measured / modeled / inferred。
- 每个阶段先以真实环境验证用户结果，再扩大执行权限或环境范围。

## 如何参与和影响路线图

- 功能想法请使用 Issues（`feature_request` 模板）或 Discussions 发起讨论。
- 较大的设计变更请先提交 RFC 形式的 Discussion，达成一致后再进入开发。
- 本文档按滚动窗口维护（通常随小版本同步更新一次）；"规划"条目在进入开发时会拆解为带验收条件的实施计划（见 [`docs/plans/`](docs/plans/current.md)）。
- 安全漏洞请勿公开发 Issue，按 [`SECURITY.md`](SECURITY.md) 披露。
