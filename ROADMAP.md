# Bittune 路线图

简体中文 ｜ [English](ROADMAP.en.md)

> 本文描述 Bittune 的产品定位与演进方向。阶段以"用户可获得的结果"划分，不绑定具体日期；条目保持主题粒度，方便展开或收拢。最近更新：2026-08-28。

## Bittune 是什么

Bittune 是运行在你自己 GPU 机器上的开源推理工程智能体：用自然语言说清目标，它替你完成环境检查、模型选择、vLLM/SGLang 部署、性能压测和参数调优，并把每一步的结论记录成可复核的执行证据。

产品由两部分组成：

- **Bittune Local**（当前开源）：运行在用户 GPU 设备上的对话式智能体，本地完成检测、部署、压测、调优和证据记录。
- **BitTune Cloud**（规划中）：可选连接的云端平台，提供认证的模型/引擎/工具组合目录、多设备管理、社区与可复核排行榜、经验知识回传，以及模型服务的 Router 上架。

核心原则只有一条：**云端提供事实，客户端真实执行。** 云端目录、社区经验、排行榜和知识包都只是候选，任何部署和调优动作都发生在用户设备上、经用户确认、由受管工具执行；原始 Prompt、模型输出、密钥和数据集始终留在设备上。

状态图例：

| 标记 | 含义 |
|---|---|
| ✅ 已发布 | 在正式 Release 中可用 |
| 🚧 进行中 | 当前开发主线的活动目标 |
| 📋 规划 | 已纳入版本方向的明确承诺 |
| 💭 探索 | 方向成立但尚无进入条件，欢迎讨论 |

## ✅ 已发布

以最近一次 [Release](https://github.com/bitcloud-ai/Bittune/releases) 为准，当前基线包括：

- Linux x86_64 单机安装器、离线安装路径与 `bittune doctor` 环境自检。
- 多推理引擎（当前覆盖 vLLM 与 SGLang）的统一受管生命周期：受限配置创建、就绪检查、端点探测、日志读取与停止。
- 统一口径的性能压测与实测运行点（MeasuredOperatingPoint）推导。
- Run Record / Artifact 形式的执行证据：敏感字段自动脱敏，失败同样留痕。
- 调优实验的记录与比较，所有结论绑定环境指纹与配置 Hash。
- TUI 交互与会话恢复；可选接入管理员配置的只读 MCP 参考源。

## 🚧 进行中

- 将环境检测、模型发现、部署与压测打磨为可独立更新的模块，让引擎与能力升级平滑落地。
- 完善本地证据的保留与授权共享：证据留在本地，导出与共享由用户显式授权并自动最小化。
- 在真实 GPU 环境完成 vLLM 与 SGLang 闭环的回归验证，随正式发布提供完整证据链。

## 📋 P1 · 本地工程闭环产品化

把"能跑"升级为"可交付、可复核、可引用"，产出机器可验证 + 人可阅读的双层调优报告——它也是后续社区分享和经验回传的基本单元。

- **领域契约公开化**：环境事实、模型画像、负载画像、部署意图、压测结果与实验记录以带版本的公共契约表达。
- **引擎适配器接口**：新推理引擎通过实现适配器接入生命周期、观测与压测体系，无需改动智能体层。
- **实验闭环一等公民**："部署 A → 基准 → 部署 B → 基准 → 对比"可回放、可引用、可跨会话追溯。
- **本地性能注册表**：直接回答"同样的硬件、同样的模型、同样的负载，以前测过什么？"
- **双层报告**：机器可校验 + 人可直接阅读的报告格式。

## 📋 P2 · BitTune Cloud 连接、认证目录与多设备管理

本地保持离线完整可用；连接云端后获得认证目录与多设备管理。本地与云端的协同方式：本地产生的执行证据经用户授权后，以摘要与签名引用同步到云端；云端下发的目录与知识包先在本地验签、经用户确认并在目标设备真实验证后，才成为可执行候选。

- **仅出站安全连接**：设备注册与数据同步只走出站加密通道，断网自动降级为纯本地模式。
- **认证组合目录**：模型（固定 Revision）/ 量化版本 / 引擎 / 工具组合的签名目录；客户端在本机重新验证兼容性后才成为候选。
- **版本与制品治理**：Agent 版本兼容性、升级策略、签名 Manifest 与制品索引（可选 Harbor 镜像分发）。
- **组织与设备管理**：用户、组织、设备与多节点组的 Web 管理入口。
- **数据授权**：用户自选数据范围，支持撤回与删除；同步内容仅为摘要与签名引用。

## 📋 P3 · 社区与可复核排行榜

围绕带签名证据的调优案例形成协作网络，让已验证的成果可被发现、复现和比较。

- **用户 / 团队 / 设备厂商主页**：公开案例、贡献记录与徽章。
- **带签名证据的案例库**：模型、引擎、设备、基准、调优结果与适用条件；案例事实以签名证据为准，讨论与点赞围绕案例交流展开。
- **在我的设备上发起复现**：网页生成复现请求，回到本地 CLI 重新检测、确认、执行并生成新证据。
- **同类设备排行榜**：按"GPU 型号/数量/拓扑 + 模型/Revision/量化 + 引擎/版本 + 测试标准 + 环境指纹"形成可比组，比较吞吐、时延、显存余量、稳定性与调优改善率；排行榜收录可比组内、带签名证据的结果。
- **隐私自选**：私有、租户内、公开三类可见范围，用户决定是否公开身份。

## 📋 P4 · 知识回传、持续学习与 Router

把经审核的经验变成下一次推荐的先验，把经验证的模型服务接入商业供给。

- **经验分类建模**：成功、无收益、失败经验分开建模，失败经验形成故障规避知识。
- **知识流水线**：经验聚类 → 知识候选 → 质量/隐私/安全/许可证审批 → 签名知识包 → 5%→25%→100% 灰度下发，可撤销、可回滚。
- **客户端再次验证**：任何知识包和云端先验都只产生候选，客户端必须重新检测并由用户确认后真实验证。
- **推荐改进**：优先使用可解释规则与可比样本组统计；排序模型离线训练与影子评估属后续规划。
- **Router 上架**：经生产验收的模型服务可申请进入 Router 沙箱验证（接口、路由、计量与对账），生产上架与结算属独立商业阶段。

## 🌤 更远期主题

以下主题在 P1–P4 推进过程中按真实使用反馈排期：

- **多卡与拓扑感知**：NUMA、卡间互联与通信瓶颈作为一等领域事实，支撑并行策略诊断与建议。
- **分布式服务形态**：分离式 Prefill/Decode 等形态的规划、部署与评测。
- **有先验的搜索**：先预测数百个配置，再只真实验证最有价值的少数候选。
- **开放的数据交换格式**：匿名化基准数据与硬件画像的导入导出。
- **更多加速硬件与操作系统**：NVIDIA 之外的硬件、Windows / macOS 宿主体验。
- **本地 Web 控制台可视化**与第三方智能体组件的插件接入。

## 产品原则

- 结论一律来自真实测量；先验、预测和社区经验必须经过目标设备真实验证并记录偏差。
- 执行权永远在客户侧：所有真实操作经由可授权、可取消、可恢复、可回滚的受管工具执行。
- 每个重要结论都标注来源与证据；不可验证的内容明确标注为 estimated。
- 数据最小化：原始 Prompt、模型输出、密钥和数据集默认留在设备上。

## 如何参与和影响路线图

- 功能想法请使用 Issues（`feature_request` 模板）或 Discussions 发起讨论。
- 较大的设计变更请先提交 RFC 形式的 Discussion，达成一致后再进入开发。
- 本文档按滚动窗口维护（通常随小版本同步更新一次）；"规划"条目在进入开发时拆解为带验收条件的实施计划。
- 安全漏洞请勿公开发 Issue，按 [`SECURITY.md`](SECURITY.md) 披露。
